Build with:

```sh
$ ./build.sh
```

Run the back-end

```sh
$ bin/goapi -a 0.0.0.0:7000 server
```

Run the front-end

```sh
$ cd web && elm reactor
```

Open http://localhost:8000 and click on `src/Main.elm` file to start
